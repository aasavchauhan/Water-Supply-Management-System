# Copy this file to `.env` for local development only.
# Do not commit real credentials.

# App-level environment toggles
APP_ENV=development
LOG_LEVEL=info

# Future server/cloud integrations (if introduced later) should use placeholders
# like the following and never real values:
# BACKEND_URL=https://api.example.com
# API_KEY=your_api_key_here
